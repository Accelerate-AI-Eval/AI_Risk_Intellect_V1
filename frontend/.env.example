# Optional. No trailing slash. Include the versioned API path.
# Direct to API (Settings test connection, auth, etc.):
VITE_BASE_URL=http://localhost:5005/api/v1
# Same-origin via Vite proxy (see vite.config.ts): VITE_BASE_URL=/api/v1
