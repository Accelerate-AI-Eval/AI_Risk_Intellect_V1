# Optional. No trailing slash. Prefer origin only: http://localhost:5005
# Or API root with version: http://localhost:5005/api/v1 (paths like /api/auth/login are normalized).
# If unset, same-origin + Vite `/api` proxy in dev.
VITE_BASE_URL=http://localhost:5005
